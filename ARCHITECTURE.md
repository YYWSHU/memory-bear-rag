# 记忆承载 RAG 知识库 — 项目架构文档

## 1. 项目概述

### 1.1 目标

将微信公众号"记忆承载"（作者：碧树西风）的 91 篇 PDF 文章构建为**三层智能知识系统**，能够**内化作者的思维模式**，以碧树西风本人的口吻、逻辑和价值观回答问题。

核心设计理念：**不是"引用文章"的 RAG，而是"知识内化为思维"的认知模拟系统**。

### 1.2 输入与输出

| 输入 | 输出 |
|------|------|
| 91 篇 PDF（2018-2025） | Web 聊天界面 |
| 用户自然语言提问 | 西风风格的流式回答 + 思维链 |
| 多轮对话历史 | 会话管理 + 分支回退 |

---

## 2. 系统架构

```
┌──────────────────────────────────────────────────┐
│                  Streamlit Web 界面                │
│   会话管理 · 分支回退 · 思维链实时流 · 来源溯源     │
└──────────────────────┬───────────────────────────┘
                       │
          ┌────────────┴────────────┐
          │                         │
     ┌────▼─────┐           ┌──────▼──────┐
     │ 检索层    │           │  生成层      │
     │ 图谱引导  │  ──────→  │  人格驱动    │
     └────┬─────┘           └──────┬──────┘
          │                         │
   ┌──────┴──────┐          ┌──────▼──────┐
   │ 向量检索     │          │ DeepSeek V4 │
   │ Ollama bge-m3│         │ Pro API     │
   │ ChromaDB     │          │ (流式输出)   │
   └──────┬──────┘          └─────────────┘
          │
   ┌──────┴──────────────────────────┐
   │         三层数据底座              │
   │                                  │
   │  Layer 3: 思维结构               │
   │  ┌────────────────────────┐     │
   │  │ 论证链 / 分析框架 / 价值锚点 │  │
   │  │ 修辞策略 / 关键词 / 隐含前提 │  │
   │  └──────────┬─────────────┘     │
   │             │                    │
   │  Layer 2: 知识图谱               │
   │  ┌────────────────────────┐     │
   │  │ 1362 概念节点 / 20401 边  │    │
   │  │ 概念共现 · 框架关联 · 权重   │  │
   │  └──────────┬─────────────┘     │
   │             │                    │
   │  Layer 1: 语义向量库             │
   │  ┌────────────────────────┐     │
   │  │ 5704 文本块 / 1024维向量  │    │
   │  │ ChromaDB / bge-m3 嵌入    │    │
   │  └──────────┬─────────────┘     │
   │             │                    │
   │     54 篇 PDF 原文 (287万字符)    │
   └─────────────────────────────────┘
```

### 2.1 三层数据底座详解

#### Layer 1: 语义向量库

| 组件 | 技术 | 详情 |
|------|------|------|
| 文本分块 | 语义段落分块 | 1000 字符/块，200 字符重叠 |
| 嵌入模型 | Ollama bge-m3 | 1024 维，GPU (RTX 4050) 加速 |
| 向量存储 | ChromaDB | 5704 条，cosine 相似度 |
| 批次策略 | 每批 20 条 | 平衡速度与 Ollama API 吞吐 |

**设计决策：**
- 选择 bge-m3 而非 bge-large：m3 多语言优化，对中文长文本效果更好
- 通过 Ollama 本地部署而非 sentence-transformers：利用 GPU 加速，避免重复加载模型
- 语义段落分块而非固定长度：保持论证完整性，避免在论证链中切断

#### Layer 2: 知识图谱

| 组件 | 技术 | 详情 |
|------|------|------|
| 节点来源 | DeepSeek V4 关键词提取 | 每篇文章 20-30 个关键词 |
| 节点数 | 1362 | 覆盖经济/社会/哲学/职场/投资等领域 |
| 边数 | 20401 | 概念共现（出现在同一篇文章） |
| 权重 | 共现文章数 | 用于检索时概念扩展 |

**设计决策：**
- 从思维结构提取关键词（而非规则匹配）：关键词更精准，反映作者真实用词
- 概念共现作为关系：简洁但有效，两个概念在同一篇文章中讨论说明存在逻辑关联
- 图谱不直接展示给用户：作为检索增强的后端知识网络

**Top 20 高频概念：**

| 概念 | 文章数 | 概念 | 文章数 |
|------|--------|------|--------|
| 信息不对称 | 11 | 普鲁士教育 | 8 |
| 预期 | 8 | 杠杆 | 7 |
| 价值 | 5 | 风险管理 | 5 |
| 工具人 | 5 | 博弈 | 5 |
| 生态位 | 4 | 人性 | 4 |
| 爆仓 | 4 | 风险 | 4 |
| 美联储 | 4 | 数学期望 | 4 |
| 三体 | 4 | 概率 | 4 |
| 生态链 | 4 | 耗材 | 3 |
| 性价比 | 3 | 接盘 | 3 |

#### Layer 3: 思维结构

| 组件 | 详情 |
|------|------|
| 提取方法 | DeepSeek V4 Pro 逐篇深度分析 |
| 覆盖 | 53/54 篇文章 |
| 每篇提取 | 核心论点 · 论证链(前提→推理→结论) · 分析框架 · 隐含前提 · 价值判断 · 思维跃迁 · 修辞策略 · 关键词(20-30个) |
| 元模式聚合 | 10 个高频框架 · 50 个核心词汇 · 10 种修辞策略 |

**53 个分析框架示例（每篇文章的框架名均由 DeepSeek 独立命名）：**

- 利益分析法+对比辟谣法
- 认知分层与生态博弈论
- 概率-时间双维量化博弈框架
- 儒法道三家规则博弈框架
- 人性底层逻辑与利益博弈分析
- 风险投资式的个人财务评估框架
- ...

**框架多样性说明：** 每篇文章的框架名称不同（因为 DeepSeek 每篇独立分析），但都共享底层思维模式：利益分析法、博弈论视角、预期管理、认知分层等。这些模式通过 System Prompt 中的"思维特征"统一注入。

---

## 3. 数据流水线 (Pipeline)

```
  PDF 源文件
  (91个, /mnt/d/yyw/download/记忆承载/)
      │
      ▼
  ┌──────────────┐
  │ Step 1: 提取  │  PyMuPDF (fitz) + pdfplumber
  │ 去重 → 文字   │  多进程并行, 8核
  └──────┬───────┘
         │ 54篇文字 (287万字符)
         │ 37篇图片型 (无法OCR, 需云端方案)
         ▼
  ┌──────────────┐
  │ Step 2: 分块  │  语义段落分块
  │ 1000字/块    │  200字重叠
  └──────┬───────┘
         │ 5704 块
         ▼
  ┌──────────────┐
  │ Step 3: 嵌入  │  Ollama bge-m3 (GPU)
  │ → ChromaDB   │  1024维, 5704条
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ Step 4: KG    │  思维结构 → 关键词提取
  │ 概念共现图谱  │  1362 节点, 20401 边
  └──────┬───────┘
         │
         ▼
  ┌──────────────┐
  │ Step 5: 思维  │  DeepSeek V4 Pro API
  │ 53篇深度提取  │  论证链+框架+修辞
  └──────────────┘
```

### 3.1 运行命令

```bash
# 完整流水线
python src/pipeline.py --step all

# 分步运行
python src/pipeline.py --step pdf      # PDF文字提取
python src/pipeline.py --step chunk    # 文本分块
python src/pipeline.py --step embed    # 向量嵌入
python src/pipeline.py --step kg       # 知识图谱
python src/pipeline.py --step thinking # 思维结构

# 端到端测试
python src/pipeline.py --step test --query "怎么跨越阶层？"
```

---

## 4. 生成流水线 (Inference Pipeline)

### 4.1 图谱引导检索

```
用户问题
    │
    ├──→ 知识图谱：匹配相关概念 + 1跳扩展
    │     "房价" → "通胀" → "美联储" → "预期管理" → ...
    │
    ├──→ 向量检索：bge-m3 语义匹配 Top-10 文本块
    │
    └──→ 思维结构：匹配相关分析框架
          │
          ▼
    构建"西风的记忆"上下文（不是"参考资料"）
```

### 4.2 人格 System Prompt

从 53 篇思维结构中提炼，核心要素：

1. **身份声明**：你就是碧树西风本人，观点来自你多年的思考，不是"某篇文章说的"
2. **思维框架**：利益分析法、博弈论视角、人性底层逻辑、先破后立
3. **核心词汇**：50 个高频概念作为思考基石
4. **修辞策略**：口语化、类比映射、极端场景推演、利益计算、从众批判
5. **铁律**：绝对不说"根据文章"、"资料显示"、"原文提到"

### 4.3 流式输出

```
DeepSeek V4 Pro API (stream=True)
    │
    ├──→ reasoning_content (思维链) → 实时展开面板
    │     "用户问了房价问题...我需要从利益分配角度分析..."
    │
    └──→ content (正式回答) → 实时流式显示
          "你问我房价会不会跌，我先问你一个问题..."
```

---

## 5. 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| PDF 提取 | PyMuPDF (fitz) + pdfplumber | 文字提取 + OCR |
| 文本分块 | 自研语义段落分块器 | 保持论证完整性 |
| 嵌入模型 | Ollama bge-m3 (1024维) | 本地 GPU 向量化 |
| 向量数据库 | ChromaDB | 持久化存储 + 相似度检索 |
| 知识图谱 | NetworkX + 思维结构关键词 | 概念网络 |
| 思维提取 | DeepSeek V4 Pro API | 论证链/框架/修辞分析 |
| 生成模型 | DeepSeek V4 Pro API (stream) | 流式对话生成 |
| Web 界面 | Streamlit | 聊天 UI |
| 会话存储 | JSON 文件 | 轻量持久化 |

### 5.1 硬件需求

| 组件 | 最低 | 推荐 |
|------|------|------|
| CPU | 4 核 | 8 核 |
| RAM | 8 GB | 16 GB |
| GPU | 无（CPU 嵌入也可） | NVIDIA 6GB+ VRAM |
| 磁盘 | 5 GB | 10 GB |

---

## 6. 项目文件结构

```
memory-bear-rag/
│
├── config.py                    # 全局配置（路径/API/模型参数）
├── requirements.txt             # Python 依赖
├── .env                         # API Key (不提交)
│
├── src/                         # 核心源码
│   ├── pdf_processor.py         # PDF 提取、去重、清洗
│   ├── text_chunker.py          # 语义段落分块
│   ├── embedder.py              # 嵌入抽象层（sentence-transformers 版）
│   ├── knowledge_graph.py       # 知识图谱（规则版 + LLM版）
│   ├── thinking_structure.py    # 思维结构提取（DeepSeek API）
│   ├── retriever.py             # 多阶段检索器（向量+图谱+思维）
│   ├── generator.py             # 生成器（DeepSeek API + 人格Prompt）
│   ├── pipeline.py              # 统一流水线脚本
│   ├── app_v4.py                # Web 界面 v4（知识内化 + 图谱驱动）
│   ├── app_v3.py                # Web 界面 v3（会话管理 + 思维链）
│   ├── app_v2.py                # Web 界面 v2（初版）
│   └── app_simple.py            # Web 界面 v1（最简版）
│
├── scripts/                     # 辅助脚本
│   ├── extract_all_thinking.py  # 批量思维结构提取
│   ├── parallel_ocr.py          # 多进程 OCR
│   └── fix_ocr_pdfplumber.py    # pdfplumber OCR 补救
│
├── data/                        # 数据目录（部分不提交）
│   ├── raw_pdfs/                # PDF 源文件副本
│   ├── extracted_texts/         # 提取的文字（JSON）
│   ├── chunks/                  # 分块结果
│   │   └── all_chunks.json      # 5704 个文本块
│   ├── chroma_db/               # ChromaDB 向量索引
│   ├── knowledge_graph/
│   │   └── knowledge_graph.json # 1362 节点 / 20401 边
│   ├── thinking_patterns/
│   │   ├── thinking_patterns.json  # 53 篇思维结构
│   │   └── meta_patterns.json      # 元模式（聚合）
│   └── sessions/                # 对话会话（JSON）
│
├── scripts/                     # 辅助脚本
├── REQUIREMENTS.md              # 需求迭代记录
└── README.md                    # 项目说明
```

---

## 7. 核心设计决策

### 7.1 为什么是"三层"而非单一 RAG？

| 层级 | 解决什么问题 | 如果去掉会怎样 |
|------|------------|--------------|
| RAG (L1) | "他说过什么"——语义相似内容检索 | 回答与文章无关 |
| 知识图谱 (L2) | "概念之间怎么关联"——结构化推理路径 | 检索碎片化，缺乏脉络 |
| 思维结构 (L3) | "他是怎么想的"——论证模式和价值观 | 回答风格不像西风 |

### 7.2 为什么内化知识而非引用原文？

传统 RAG 的问题是：LLM 说"根据文章..."，这破坏了人物模拟的沉浸感。

**解决方案：** System Prompt 声明"你就是西风本人"，检索到的内容作为"你的记忆"注入，而非"参考资料"。生成时 LLM 自然会用第一人称表达这些观点。

### 7.3 为什么用 Ollama 本地嵌入而非 API？

- **速度**：本地 GPU 嵌入比 API 调用快（无网络延迟）
- **成本**：5704 条嵌入，API 调用累计成本可观
- **可控**：模型版本固定，不受 API 变更影响
- **RTX 4050 (6GB)**：足够加载 bge-m3 并同时运行推理

### 7.4 为什么独立提取思维结构而非让生成时实时分析？

- **效率**：53 篇文章的一次性分析 vs 每次查询时重新分析
- **一致性**：预提取的框架/关键词确保稳定的角色人格
- **可迭代**：元模式可以手工审查和调整

---

## 8. API 接口说明

### 8.1 Ollama Embedding API

```
POST http://localhost:11434/api/embed
{
  "model": "bge-m3",
  "input": ["文本1", "文本2", ...]
}
→ { "embeddings": [[...1024维...], ...] }
```

### 8.2 DeepSeek Chat API

```
POST https://api.deepseek.com/v1/chat/completions
{
  "model": "deepseek-v4-pro",
  "messages": [...],
  "stream": true,
  "temperature": 0.7,
  "max_tokens": 2000
}
→ stream {
    "choices": [{
      "delta": {
        "reasoning_content": "思维链...",  // 推理过程
        "content": "回答..."               // 正式回答
      }
    }]
  }
```

---

## 9. 已知限制与改进方向

| 限制 | 状态 | 改进方案 |
|------|------|---------|
| 37 篇图片 PDF 无法提取 | 待解决 | 云端 OCR API（百度/腾讯） |
| 每篇文章框架名不统一 | 设计特性 | 可添加二级归类 |
| 知识图谱只有共现关系 | 可用 | LLM 深度实体关系抽取 |
| 83 个技能占 12k tokens | 外部因素 | 清理不需要的技能 |
| ChromaDB 文件 68MB 超 GitHub 限制 | 可忽略 | Git LFS 或 .gitignore |

---

## 10. 快速开始

```bash
# 1. 克隆 + 安装
git clone https://github.com/YYWSHU/memory-bear-rag.git
cd memory-bear-rag
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2. 配置 API Key
echo "DEEPSEEK_API_KEY=sk-xxx" > .env

# 3. 启动 Ollama (确保 bge-m3 已下载)
ollama serve &
ollama pull bge-m3

# 4. 运行数据流水线
python src/pipeline.py --step all

# 5. 启动 Web 界面
streamlit run src/app_v4.py --server.port 8501

# 6. 打开浏览器
# http://localhost:8501
```
